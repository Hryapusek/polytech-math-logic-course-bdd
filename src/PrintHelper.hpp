#ifndef PRINT_HELPER
#define PRINT_HELPER

#include "BDDHelper.hpp"

std::string to_string(bddHelper::Object obj);
std::string to_string(bddHelper::Property prop);
std::string to_string(bddHelper::Color col);
std::string to_string(bddHelper::Nation col);
std::string to_string(bddHelper::Plant col);
std::string to_string(bddHelper::Animal col);

#endif
